// Spiral Order matrix traversal.

#include <bits/stdc++.h>
using namespace std;
int main()
{
    int n, m;
    cin >> n >> m;
    int arr[n][m];
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < m; j++)
        {
            cin >> arr[i][j];
        }
    }
    int rs = 0;
    int cs = m-1;
    int re = n-1;
    int ce = 0;
    for (int i = 0; i < n; i++)
    {
        
    }
    return 0;
}